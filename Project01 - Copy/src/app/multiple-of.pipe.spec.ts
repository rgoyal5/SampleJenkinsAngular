import { MultipleOfPipe } from './multiple-of.pipe';

describe('MultipleOfPipe', () => {
  it('create an instance', () => {
    const pipe = new MultipleOfPipe();
    expect(pipe).toBeTruthy();
  });
});
